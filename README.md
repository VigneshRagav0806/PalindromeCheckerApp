/**
* This program checks whether a given string is a palindrome
* by reversing the string.
*
* It iterates from the last character to the first to build
* a reversed version of the input string and then compares
* it with the original string.
*
* If both strings are the same, the input is a palindrome.
*
* @author Developer
* @version 3.0
  */